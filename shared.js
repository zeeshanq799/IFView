const $ = n => document.getElementById(n), v = n => parseFloat($(n)?.value) || 0; function fmt(n) { return isFinite(n) && !isNaN(n) ? Math.abs(n) >= 1e6 ? "$" + (n / 1e6).toFixed(2) + "M" : Math.abs(n) >= 1e3 ? "$" + (n / 1e3).toFixed(1) + "K" : "$" + n.toFixed(2) : "$0.00" } function fmtN(n) { return isFinite(n) ? Math.round(n).toLocaleString() : "0" } function set(n, e) { const t = $(n); t && (t.classList.remove("pop"), t.textContent = e, t.offsetWidth, t.classList.add("pop"), setTimeout((() => t.classList.remove("pop")), 400)) } function updateLbl(n, e) { const t = $(n); t && (t.textContent = e) } function toggleFAQ(n) { n.closest(".faq-item").classList.toggle("open") } function copyTxt(n) { const e = $(n)?.textContent || ""; navigator.clipboard.writeText(e).then((() => { const n = event.currentTarget, e = n.textContent; n.textContent = "✓ Copied!", n.style.color = "var(--green)", setTimeout((() => { n.textContent = e, n.style.color = "" }), 1800) })) } function dlTxt(n, e) { const t = new Blob([`IncomeFromViews — ${n}\nGenerated: ${new Date().toLocaleDateString()}\nURL: ${location.href}\n\n${e || ""}`], { type: "text/plain" }), o = document.createElement("a"); o.href = URL.createObjectURL(t), o.download = n.replace(/\s/g, "_") + ".txt", o.click() } function shareIt() { navigator.share ? navigator.share({ title: "IncomeFromViews", url: location.href }) : (navigator.clipboard.writeText(location.href), alert("Link copied!")) } window.addEventListener("scroll", (() => { const n = document.documentElement, e = $("progress"); e && (e.style.width = n.scrollTop / (n.scrollHeight - n.clientHeight) * 100 + "%") })), document.addEventListener("DOMContentLoaded", (() => { const n = new IntersectionObserver((n => { n.forEach((n => { n.isIntersecting && (n.target.style.opacity = "1", n.target.style.transform = "translateY(0)") })) }), { threshold: .08 }); document.querySelectorAll(".calc-section,.blog-card,.fade-in").forEach((e => { e.style.cssText += "opacity:0;transform:translateY(20px);transition:opacity .5s ease,transform .5s ease", n.observe(e) })); const e = document.querySelector("nav"); if (e) { const n = document.createElement("button"); n.className = "menu-btn", n.innerHTML = "☰", n.onclick = () => { const e = document.querySelector(".mobile-menu"); e.classList.toggle("open"), n.innerHTML = e.classList.contains("open") ? "✕" : "☰" }, e.appendChild(n); const t = document.createElement("div"); t.className = "mobile-menu"; const navLinksContainer = document.querySelector(".nav-links"); if (navLinksContainer) { Array.from(navLinksContainer.children).forEach(child => { if (child.classList.contains("nav-link")) { const e = child.cloneNode(!0); e.className = "mobile-link", t.appendChild(e) } else if (child.classList.contains("nav-dropdown")) { const trigger = child.querySelector(".dropdown-trigger"); const content = child.querySelector(".nav-dropdown-content"); if (trigger && content) { const d = document.createElement("div"); d.className = "mobile-dropdown"; const btn = document.createElement("button"); btn.className = "mobile-link mobile-trigger"; btn.innerHTML = trigger.innerHTML; btn.onclick = () => d.classList.toggle("open"); d.appendChild(btn); const sub = document.createElement("div"); sub.className = "mobile-submenu"; content.querySelectorAll(".nav-link").forEach(l => { const c = l.cloneNode(!0); c.className = "mobile-link mobile-sublink"; sub.appendChild(c) }); d.appendChild(sub); t.appendChild(d) } } }) } const o = document.querySelector(".nav-cta"); if (o) { const n = o.cloneNode(!0); n.className = "mobile-link", n.style.marginTop = "12px", n.style.background = "var(--blue-l)", n.style.color = "var(--blue)", n.style.textAlign = "center", t.appendChild(n) } document.body.appendChild(t) } document.querySelectorAll(".btn-primary").forEach((n => { n.addEventListener("click", (() => { window.innerWidth <= 780 && setTimeout((() => { const n = document.querySelector(".card-right"); n && n.scrollIntoView({ behavior: "smooth", block: "start" }) }), 100) })) })) }));
